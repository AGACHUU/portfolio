// function HI(name) {
//   console.log(name);
// }

// if ((event.type = 'touch')) {
// } else {
// }

// function handler(event){
//     if(event.type='touch'{

//     }else{})
// }

function sayHi(name) {
  // 역할에 맞는 이름을 지어준 거임
  console.log('hi', name); // hi 이름
}

sayHi('영수');

// hi 영수

// sayHi()

document.addEventListener('click', 이자리);
// function 이자리() {

// }

// 이자리 얘는 click이 발생하면 브라우져가 event를 인자로 넣어서 호출한다 이게전부임
// 이자리(event)

// event - 이벤트객체 = 그 이벤트(이 경우 클릭)에 대한 정보들을 담고있는 객체
// event.target = 클릭이 발생한 dom element 정보, 클릭이 일어난 좌표, ...

// browser 가 handler 를 호출을 하는데,
// handler 역할을 하는 함수
// handler(event) 이렇게 event를 첫번째 인자로 담아서 호출함
// listener

// 이 handler를

// handler 이름이 중요함? 안중요함
// 함수이름을 역할에 맞게 지우는건 사람한테만 의미가 있어
// function
